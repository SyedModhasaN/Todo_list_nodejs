<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <nav class="header">
        <h1 id="heading">Todo list</h1>
    </nav>

    <div class="box">
        <form action="/" method="post" onsubmit="return validateForm()">
            <input type="text" name="ele1" id="taskInput" placeholder="Enter the Task">
            <button type="submit">+</button>
        </form>
    </div>

    <!-- <div class="box-main">
       <% for(var i=0; i< ejes.length; i++){ %>
        <div class="item">
            <input type="checkbox">    
            <p><%= ejes[i] %></p>
        </div>
       <% } %>
    </div> -->
  <div class="box-main">
  <% ejes.forEach(function(items, index){ %>
    <div class="item">
      <input type="checkbox">

      <!-- Task Text -->
      <p id="task-text-<%= index %>" class="task-text"><%= items.name %></p>

      <!-- Action Buttons (Edit + Delete) -->
      <div id="action-buttons-<%= index %>" style="display:inline;">
        <!-- Edit Button -->
        <button type="button" class="edit-btn" onclick="showEditForm(<%= index %>)">Edit</button>

        <!-- Delete Button -->
        <form action="/delete" method="post" class="delete-form" style="display:inline;">
          <button type="submit" name="delete" class="delete-btn" value="<%= items._id %>">Delete</button>
        </form>
      </div>

      <!-- Hidden Edit Form -->
      <form id="edit-form-<%= index %>" action="/edit/<%= items._id %>" method="post" class="edit-form" style="display:none; margin-top:5px;">
        <input type="text" id="edit-input-<%= index %>" name="updatedTask" value="<%= items.name %>" required>
        <button type="submit">Save</button>
        <button type="button" onclick="cancelEdit(<%= index %>)">Cancel</button>
      </form>
    </div>
  <% }) %>
</div>







    <!-- footer -->
    <footer>
        <p>Smash &copy; toDo_List<sup>&reg;</sup></p>
    </footer>

    <!-- Client-side validation -->
      <!-- load external JS -->
    <script src="/js/validation.js"></script>
      
</body>
</html>
